// src/components/PermissionExample.jsx
// This is an example component showing how to use permissions throughout your app

import React from 'react';\nimport { hasPermission, hasAnyPermission, isAdmin } from '../utils/permissions';\n\nfunction PermissionExample() {\n  return (\n    <div className=\"p-4\">\n      <h2 className=\"text-xl font-bold mb-4\">Permission Examples</h2>\n      \n      {/* Show content only if user has specific permission */}\n      {hasPermission('users.view') && (\n        <div className=\"mb-4 p-3 bg-blue-50 rounded\">\n          <p>✅ You can view users</p>\n        </div>\n      )}\n      \n      {/* Show content if user has ANY of multiple permissions */}\n      {hasAnyPermission(['users.create', 'users.edit']) && (\n        <div className=\"mb-4 p-3 bg-green-50 rounded\">\n          <p>✅ You can create or edit users</p>\n        </div>\n      )}\n      \n      {/* Show admin-only content */}\n      {isAdmin() && (\n        <div className=\"mb-4 p-3 bg-purple-50 rounded\">\n          <p>👑 Admin-only content</p>\n        </div>\n      )}\n      \n      {/* Conditional buttons based on permissions */}\n      <div className=\"space-x-2\">\n        {hasPermission('users.create') && (\n          <button className=\"px-4 py-2 bg-blue-500 text-white rounded\">\n            Create User\n          </button>\n        )}\n        \n        {hasPermission('users.edit') && (\n          <button className=\"px-4 py-2 bg-green-500 text-white rounded\">\n            Edit User\n          </button>\n        )}\n        \n        {hasPermission('users.delete') && (\n          <button className=\"px-4 py-2 bg-red-500 text-white rounded\">\n            Delete User\n          </button>\n        )}\n      </div>\n    </div>\n  );\n}\n\nexport default PermissionExample;